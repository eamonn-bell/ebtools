# ebtools

`ebtools` contains helper functions and lookup data for electricity market
data workflows, especially pandas dataframes and time series.

## Package Layout

- `ebtools.general`: dataframe, datetime, file IO, settlement period, EFA, and
  array helpers.
- `ebtools.data`: static lookup data and date format lists.
